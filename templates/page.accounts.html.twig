{% extends "layout.base.html.twig" %}

{% block additionalStyles %}
  <link rel="stylesheet" href="{{ asset('build/autocomplete.css') }}">
{% endblock %}

{% block header %}Účty{% endblock %}

{% block contents %}
  <modal-account-remove
    :analytical-account-remove-url="'{{ path("api_accounts_analytical_remove") }}'"
  >
  </modal-account-remove>
  <form-account
    :form="{{ forms.account|json_encode }}"
    :token="{{ csrf_token('form_account')|json_encode }}"
  >
  </form-account>
  <div class="row">
    <div class="col-12">
      <page-accounts
        :api-accounts-listing-url="'{{ path("api_accounts_listing") }}'"
      >
      </page-accounts>
    </div>
  </div>
{% endblock %}
